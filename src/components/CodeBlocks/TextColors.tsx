import { SBlueText, SPurpleText, SRedText } from "../Pagewrapper/styles";

export const SImport = () => { return <SRedText>import</SRedText>; };

export const SConst = () => {
  return <SRedText>const</SRedText>;
};

export const SOnClick = () => {
  return <SBlueText>onClick</SBlueText>;
}

export const SUseState = () => { return <SPurpleText>useState</SPurpleText>; };